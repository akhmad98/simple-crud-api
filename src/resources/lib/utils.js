const fs = require('fs');

// function writeDataToFile(filename, content) {
//     fs.writeFileSync(filename, JSON.stringify(content), 'urf8', (err) => {
//         if (err) {
//             c
//         }
//     })
// }
// read and check then write to file